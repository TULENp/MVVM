<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:ViewModels="clr-namespace:MVVM.ViewModels"
             x:Class="MVVM.Views.Page3">
    <ContentPage.BindingContext>
        <ViewModels:Page3VM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Button Text="Fill" Command="{Binding GetUsers}" Style="{StaticResource MyButton}"/>
                <StackLayout BindableLayout.ItemsSource="{Binding UsersCollection}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Label Text="{Binding Country}" Style="{StaticResource MyLabel}" FontSize="50"/>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                    <BindableLayout.EmptyView>
                        No info
                    </BindableLayout.EmptyView>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>