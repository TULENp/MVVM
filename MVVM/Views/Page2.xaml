<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:ViewModels="clr-namespace:MVVM.ViewModels"
             x:Class="MVVM.Views.Page2">
    <ContentPage.BindingContext>
        <ViewModels:Page2VM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Button Text="Next page" Style="{StaticResource MyButton}" Command="{Binding OnPush}"/>
                <Button Text="Fill" Style="{StaticResource MyButton}" Command="{Binding Fill}"/>
                <ListView ItemsSource="{Binding Source}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding FirstName}" Detail="{Binding LastName}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>